package com.crawler.config;

public class Config {
    // Maximum depth for crawling
    public static final int MAX_DEPTH = 1;

    // Maximum number of product URLs to collect per domain
    public static final int MAX_PRODUCTS_PER_DOMAIN = 50; //if needed
}
